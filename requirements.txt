# --- Deep Learning Core ---
# PyTorch (CUDA 버전에 맞춰 설치하는 것이 좋지만, 기본적으로 이걸로 설치됨)
torch>=2.0.0
torchvision
torchaudio

# --- NLP & Model ---
# BERT 및 사전 학습 모델 로드용
transformers>=4.30.0
# Phase 1에서 SBERT(all-mpnet-base-v2) 사용 및 유사도 계산용
sentence-transformers>=2.2.2

# --- Graph Processing ---
# Taxonomy(계층 구조) 그래프 생성 및 관리
networkx>=3.0

# --- LLM Integration ---
# Phase 1에서 GPT-4o-mini 호출용 (반드시 1.0.0 이상 버전 필요)
#openai>=1.0.0
google-generativeai>=0.3.0
python-dotenv>=1.0.0

# --- Utilities ---
# 행렬 연산 및 진행률 표시바
numpy
tqdm
scikit-learn
pandas